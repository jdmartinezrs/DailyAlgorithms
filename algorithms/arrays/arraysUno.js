
const records = ()=> {
    const recordings = ['NelsonNed', 'NelsonYSusEstrellas', 'Melódicos', 'CatorceCañonazo'];
    return recordings
}
export default records;
/*globalThis.recordingList = records();
//console.log(recordingList);
//console.log(recordingList[1]); 

const getallRecordings = () => {
  const recordingList = records();
  for (let record of recordingList) {
    console.log(record);
  }
}
//getallRecordings();

const getRecordWithForEach = ()=> {
  const recordingList = records();
  recordingList.forEach(record => {
    console.log(record);
  })
}
//getRecordWithForEach();

const getrecordswith = () =>{
  for(let tecord of recordingList) {
    console.log(tecord);
  }
}
//getrecordswith();

const getrecordings = ()=> {
  recordingList.forEach(record => {
    console.log(record);
  })
}
//getrecordings();

const diggingIntoArrays = () => {
  for(let record of recordingList){
    console.log (record)
  }
}

//diggingIntoArrays();
const pushrecord = (record)=> {
  diggingRecords.push(record);
}

//pushrecord('Anibal Velazquez');
pushrecord('Eliceo Meza');
//

const deleteLastRecord = ()=> {
  diggingRecords.pop();
}
//deleteLastRecord();

const deletefirstRecord = ()=> {
  diggingRecords.shift();
}

deletefirstRecord();

const addFirstRecord = (lp)=>{
  diggingRecords.unshift(lp);
}

addFirstRecord('Viernes Para Recordar');

console.log(diggingRecords);
*/


//METODOS DE BÚSQUEDA

/*
  globalThis.recordingList = records();
  const canYouDigIt = (name) => {
    return recordingList.includes(name);
  }
  console.log(canYouDigIt('NelsonNed'));

console.log(recordingList.indexOf('Melódicos')); // 2 
console.log(recordingList.find(record => record === 'Melódicos')); // Melódicos
console.log(recordingList.findIndex(recors=> recors === 'NelsonNed'))*/